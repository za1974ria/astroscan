# Development dependencies — pytest suite + coverage.
# Production deps are pinned in requirements.txt; this file is only for CI/CD
# and local test runs. Never installed on the production server.

pytest>=7.0,<9
pytest-cov>=4.0
pytest-mock>=3.10
