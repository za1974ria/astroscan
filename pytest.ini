[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts =
    -v
    --tb=short
    --strict-markers
    --color=yes
    -m "not prod_smoke"
markers =
    smoke: smoke tests — high-level health checks, may touch DB
    unit: unit tests — pure logic, no I/O, no Flask context
    integration: integration tests — DB / network / external services
    slow: slow tests (> 1s)
    prod_smoke: production smoke tests — hit live HTTP service, excluded by default
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
